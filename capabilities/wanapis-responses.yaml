naftiko: 1.0.0-alpha2
info:
  label: WanAPIs API — Responses
  description: 'WanAPIs API — Responses. 1 operation. Lead operation: Create response. WanAPIs-native Responses API for structured workflows, compatible with the OpenAI Responses API.'
  tags:
  - WanAPIs
  - Responses
  created: '2026-05-27'
  modified: '2026-05-27'
binds:
- namespace: env
  keys:
    WANAPIS_API_KEY: WANAPIS_API_KEY
capability:
  consumes:
  - type: http
    namespace: wanapis-responses
    baseUri: https://api.wanapis.com/v1
    description: WanAPIs API — Responses business capability.
    resources:
    - name: responses
      path: /responses
      operations:
      - name: createresponse
        method: POST
        description: Create response
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.WANAPIS_API_KEY}}'
  exposes:
  - type: rest
    namespace: wanapis-responses-rest
    port: 8080
    description: REST adapter for WanAPIs API — Responses.
    resources:
    - path: /v1/responses
      name: responses
      description: REST surface for responses.
      operations:
      - method: POST
        name: createresponse
        description: Create response
        call: wanapis-responses.createresponse
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: wanapis-responses-mcp
    port: 9090
    transport: http
    description: MCP adapter for WanAPIs API — Responses.
    tools:
    - name: create-response
      description: Create response
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: wanapis-responses.createresponse
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
